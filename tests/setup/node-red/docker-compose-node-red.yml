services:
  node-red-test:
    image: statuscompliance/node-red-status:latest
    container_name: node-red-testing
    ports:
      - "1880:1880"
    environment:
      - NODE_RED_USERNAME=admin
      - NODE_RED_PASSWORD=admin
      - NODE_RED_ENABLE_PROJECTS=false
      - TZ=Europe/Madrid
    restart: "no"
    networks:
      - node-red-network

networks:
  node-red-network:
    driver: bridge